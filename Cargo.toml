[workspace]
members = [
    "programs/trade-escrow",
    "programs/prediction-market"
]
resolver = "2"

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1

[profile.release.build-override]
opt-level = 3
